# AGENTS

## 项目目标
- 实现一个优化版本的 hapi，使用户可以通过网页控制服务器上的 agent，包括 Codex 和 Claude。

## 治理与上下文入口

- `CLAUDE.md` 是项目记忆入口（启动纪律、目录索引、关键约定）。
- `.claude/constitution.md` 是不可变底线；`.claude/rules/` 是可进化规则库。
- `docs/AGENTS.md` 负责长期文档、索引与沉淀治理；`docs/project.md` 是项目认知入口。
